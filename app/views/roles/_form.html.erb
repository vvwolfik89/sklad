<div class="row">
  <div class="col-md-12">
    <%= custom_form_for(@role, bootstrap3_form_options) do |f| %>
      <div class="bs-component">
        <div class="panel">
          <div class="panel-body">
            <%= f.input :title %>
            <%= f.input :description %>
            <%= f.input :role_type, collection: Role.role_type_attributes_for_select, include_blank: false, class: " select2-auto"  %>
            <div class="row">
              <div class="col-sm-offset-3 col-sm-2 p-3">
                <%= f.button :submit, "Сохранить", class: "btn-primary btn-block" %>
                <%= link_to("Все роли", roles_path, class: "btn btn-dark btn-block") %>
              </div>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
